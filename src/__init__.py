"""
Shared library for the Jet Engine Hospital project (NASA C-MAPSS).

This package is imported by BOTH the training notebook and the deployed app
(app/app.py), so that inference logic is guaranteed identical between offline
evaluation and the live dashboard (app parity requirement).
"""
